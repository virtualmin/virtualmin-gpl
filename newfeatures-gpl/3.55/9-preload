desc=Preload libraries option
html=Ported the Module Config option to preload Virtualmin libraries from the Pro version, which can speed up the web-based UI. This is not enabled by default on regular manual installs though.
link=/config.cgi?virtual-server
master=1
