desc=Shared IPv6 addresses
html=Multiple virtual servers can now share a single IPv6 address, just as can be done for IPv4. Each domain can either not use IPv6 at all, use one of several shared addresses, or have its own private address.
master=1
link=newip_form.cgi?dom=${ID}
